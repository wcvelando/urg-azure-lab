# environments/prod/backend.tf.example
# Copiar como backend.tf y completar. backend.tf está en .gitignore.
# NOTA: prod usa un key diferente al lab → states completamente separados
terraform {
  backend "azurerm" {
    resource_group_name  = "rg-terraform-state"
    storage_account_name = "COMPLETAR_STORAGE_ACCOUNT"
    container_name       = "tfstate"
    key                  = "prod/student-records.tfstate"
  }
}
